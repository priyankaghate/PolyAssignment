<div class="container">
	<div class="jumbotron">
	  <center>
		<h1><%= @post.title %></h1>
		<p>
			<%= @post.content %>
		</p>
	  </center>
	  <%=render 'comments/form', :commentable => @post %>
	  <%#=link_to "Comment", new_comment_path, class: "btn btn-primary"%>
	</div>
</div>